import React, { useState } from 'react';
import { ListRepositories } from './components/ListRepositories';


const App: React.FC = () => {
  
  return (
    <div className="App">
      <ListRepositories  />
    </div>
  );
};

export default App;
